---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<header class="reveal mb-[clamp(2.5rem,6vw,4.5rem)]">
  <h1
    class="text-[clamp(2rem,1.4rem_+_3vw,3.4rem)] font-medium leading-[1.15] tracking-[-0.02em]"
  >
    {title}
  </h1>
</header>
